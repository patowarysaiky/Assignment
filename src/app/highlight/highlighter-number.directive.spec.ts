import { HighlighterNumberDirective } from './highlighter-number.directive';

describe('HighlighterNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighterNumberDirective();
    expect(directive).toBeTruthy();
  });
});
